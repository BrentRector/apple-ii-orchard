# Linker config for COPY_6502.s -- embedded 6502 track-copy driver of COPY.COM
# (2.20 44K). Runs at $14AE (the .COM stages it +$1000 from file $04AE); 82 bytes.
MEMORY {
    RAM: start = $14AE, size = $0052, file = %O;
}
SEGMENTS {
    CODE: load = RAM, type = ro;
}
