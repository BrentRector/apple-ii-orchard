# Linker config for FORMAT_6502.s -- embedded 6502 engine of FORMAT.COM.
# Run address $1400 (Z-80 stages LD HL,$1400 as the 6502 entry).
MEMORY {
    RAM: start = $1400, size = $0600, file = %O;
}
SEGMENTS {
    CODE: load = RAM, type = ro;
}
